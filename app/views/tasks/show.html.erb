<div class="row justify-content-center">
  <div class='col-6 mt-5'>
    <h1 class='text-center'><%= @task.title %>
      <% if @task.completed %>
        <i class="fa fa-check" aria-hidden="true"></i>
      <% end %>
    </h1>
    <p class='mt-4 text-center'><%= @task.details %></p>
  </div>
</div>